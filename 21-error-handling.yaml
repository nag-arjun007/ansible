- name: demo on error handlimg
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: check roboshop user exist or not
    command: id roboshop
    register: user_output
    ignore_errors: true

  - name: create user
    command: useradd roboshop
    when: user_output.rc != 0  