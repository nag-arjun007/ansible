- name: check number of live is_live_sessions
  hosts: local
  connection: local
  vars:
    live_sessions: 6
  tasks:
  - name: is ansible more than 5 live_sessions
    debug:
      msg: "yes, ansible took more than 5 sessions"
    when:  live_sessions > 5  